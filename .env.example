# Keep this file in the repo (no real secrets here).
# Copy the variable name into Vercel → Settings → Environment Variables,
# and paste your real Resend key there — NOT in the code.

RESEND_API_KEY=re_your_key_here
